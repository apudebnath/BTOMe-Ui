import React from 'react';
import MenuBar from '../../../Shared/MenuBar/MenuBar';

const Home = () => {
    return (
        <div>
            <MenuBar></MenuBar>
        </div>
    );
};

export default Home;